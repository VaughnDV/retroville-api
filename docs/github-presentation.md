# GitHub presentation

Suggested public metadata:

- Description: `Django API for activity matching, recovered from 2019 and modernised to Django 5.2 LTS`
- Topics: `django`, `django-rest-framework`, `celery`, `channels`, `postgresql`, `python`
- Social preview: a screenshot of `/api/docs/` against the local Compose stack
