# Contributing

This repository is a personal engineering showcase. Outside contributions are
welcome as discussion, but there is no commitment to merge them.

1. Install with `make install`.
2. Run `make lint typecheck test` before opening a pull request.
3. Do not add paid-API credentials to tests or fixtures.
4. Prefer characterisation tests when changing matching behaviour.
